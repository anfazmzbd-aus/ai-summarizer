canonical path now executes through V9 pipeline;
V9 remains provider-independent;
provider execution still happens through the existing service;
usage is accumulated across pipeline calls;
strategy/chunk metadata is exposed read-only;
V10 intelligence is still not integrated;
compatibility /summarize remains unchanged.